document.getElementById('loginForm').addEventListener('submit', async (e)=>{e.preventDefault();const fd=new FormData(e.target);const box=document.getElementById('loginMessage');box.className='notice';box.textContent='Accesso in corso...';try{const data=await API.login(fd.get('email'),fd.get('password'));if(data.ok){setSession(data.user);location.href='dashboard.html';}else{box.className='notice error';box.textContent=data.error||'Credenziali non valide';}}catch(err){box.className='notice error';box.textContent='Errore di collegamento. Controlla Apps Script.';}});
