document.getElementById('trackingForm').addEventListener('submit',async(e)=>{e.preventDefault();const code=document.getElementById('trackingCode').value.trim();const box=document.getElementById('trackingResult');box.innerHTML='<div class="notice">Ricerca pratica...</div>';try{const data=await API.listReports();const r=(data.reports||[]).find(x=>String(x.id).toLowerCase()===code.toLowerCase());if(!r){box.innerHTML='<div class="notice error">Pratica non trovata.</div>';return;}box.innerHTML=`<div class="card"><span class="badge">${r.stato||'Nuova'}</span><h2>${r.titolo||r.categoria}</h2><p class="meta">${r.id} • ${r.quartiere||''} • ${r.data||''}</p><p>${r.descrizione||''}</p><p><strong>Indirizzo:</strong> ${r.indirizzo||''}</p><p><strong>Referente:</strong> ${r.referenteNome||'Non ancora assegnato'}</p></div>`;}catch(err){box.innerHTML='<div class="notice error">Errore di collegamento.</div>';}});
